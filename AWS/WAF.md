AWS WAF - Web Application Firewall. 

this lets you monitor HTTP(S) requests that are forwarded to your resources. You can attach this directly to multiple resources like an ALB. 

It lets your control access to your content via specific criteria e.g. IP address 